{% extends 'base.html.twig' %}

{% block title %}Edit Trick{% endblock %}

{% block main %}
    <section class="my-4">
    <h1 class="text-center">Modifier Trick {{ trick.name }}</h1>

    <div class="d-flex justify-content-between">
    <a href="{{ path('app_trick_show', {'id':trick.id, 'page':1}) }}" class="btn btn-lg m-4"><i class="fa-solid fa-arrow-left-long"></i></a>
        {{ include('trick/_delete_form.html.twig') }}
    </div>
    <hr>
    {{ include('trick/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </section>
{% endblock %}
