<form method="post" action="{{ path('app_trick_delete', {'id': trick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="my-auto">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
    <button class="btn btn-delete link-danger deledit fs-3"><i class="fa-solid fa-trash-can"></i></button>
</form>
